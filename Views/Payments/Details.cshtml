@model SkaleFitnessMVC.Models.Payment

<style>
    .card {
        max-width: 450px;
        background: #f9fafb;
        padding: 25px;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.08);
    }

    p {
        margin: 6px 0;
        font-size: 15px;
    }

    .btn-back {
        background: #6b7280;
        color: white;
        padding: 8px 18px;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        margin-top: 15px;
    }

        .btn-back:hover {
            background: #4b5563;
        }
</style>

<h2>Payment Details</h2>

<div class="card">
    <p><b>Payment ID:</b> @Model.PaymentId</p>
    <p><b>Member ID:</b> @Model.MemberId</p>
    <p><b>Amount:</b> ₹@Model.Amount</p>
    <p><b>Payment Date:</b> @Model.PaymentDate.ToShortDateString()</p>
    <p><b>Payment Method:</b> @Model.Method</p>

    <button type="button" class="btn-back" onclick="history.back()">Back</button>
</div>
